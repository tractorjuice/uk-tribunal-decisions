# Runtime dependencies for the scraping and build pipeline.
#
# Pinned deliberately. pdfplumber's text extraction output changes between
# versions, and every regex in extract_structured_fields.py is calibrated
# against whatever version produced the current corpus — an unpinned upgrade
# silently changes thousands of extracted fields.
#
# Requires Python 3.10 or later (the scripts use PEP 604 `X | None` syntax).

requests==2.32.3
pdfplumber==0.11.4
